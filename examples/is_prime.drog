(
Do
  (Let offsets (Cons 1 (Cons 7 (Cons 11 (Cons 13 (Cons 17 (Cons 19 (Cons 23 (Cons 29 (- 0 1) ) ) ) ) ) ) ) ) )

  (Func gen-primes n)
    (Do
      (Call gen-primes-inner n offsets)
    )

  (Func gen-primes-inner n offsets)
    (Do
      (Let offset (Car offsets))
      (If (< 0 offset)
        (Cons (+ (* 30 n) offset) (Call gen-primes-inner n (Cdr offsets)))
        null
      )
    )

  (Call gen-primes 228)

)
